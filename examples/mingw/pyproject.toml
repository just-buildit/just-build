[build-system]
requires = ["just-buildit"]
build-backend = "just_buildit"

[project]
name = "add"
version = "0.1.0"
description = "just-buildit MinGW UCRT64 example."
requires-python = ">=3.8"

[tool.just-buildit]
command = "make"
repair = false
