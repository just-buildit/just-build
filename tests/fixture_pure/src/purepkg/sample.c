/* sample.c — shipped as package data, NOT an extension source.
 * Deliberately not compilable as a standalone extension: pure=true must
 * skip it entirely rather than trying to build it. */
this is not valid C and must never reach a compiler
